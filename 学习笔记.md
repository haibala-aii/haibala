# haibala · 学习笔记（M1 → M4.5 合集）

> 这份笔记按里程碑记录你**做了什么**、**学到什么**、**怎么验证**。
> 目标：让你不仅是"写出代码"，而是**理解 agent 是怎么一步步搭起来、并学会自我改进的**。

---

## 总览：你最终做出了什么

一个**本地优先、开源的"多 Agent 决策 + 评测"中枢（haibala）**。
它站在多个 agent 之上：观察任务 → 决策(用谁/拆不拆/优先级/预算) → 同场比试 → rubric 打分 → **边用边学会越来越会做这些决策**，并提供监控/审批/审计界面。

核心主线：**observe → decide → dispatch → judge → learn**（这就是一切 agent 的骨架）。

---

# M1 —— 先跑通「监控 + 决策闭环」

## 概念
- **接口/插件化**：所有 agent 统一 `submit(task)->artifact`，核心只认接口，不认具体工具（借鉴 DSH 的插件思想）。
- **抽象/依赖反转**：上层不关心"Codex 还是 DeepSeek"，只管调用统一接口。
- **特征工程(observe)**：把文字需求抽成结构化 `features`。
- **rubric + judge**：带权重的评分维度 + 裁判打分（**这系统最值钱、也最难的一环=评估**）。
- **案例记忆(L1)**：每次跑完存「特征→决策→实际得分→成本」，下次检索推荐。
- **闭环**：observe→decide→dispatch/benchmark→judge→learn。

## 落地（纯 Python，零依赖）
```
core/features.py   observe：文本 -> 特征
core/pipeline.py   闭环：把各块串起来
core/store.py      sqlite：工单/评测/案例
workers/           WorkerPlugin 接口 + mock + 注册表
judge/             JudgePlugin 接口 + DEFAULT_RUBRIC + MockJudge
learn/router.py    决策：检索案例推荐 worker/拆分/优先级/预算
main.py            命令行 + 本地 Web(入口)
web/dashboard.html 界面
```

## 关键现象（"越用越会"的最小证据）
连续跑两次同类任务，第二次 `source` 变为"参考 1 个相似案例"——**它开始用自己上轮存的记录了。**

## 运行
```bash
python main.py --run "批量抠图小程序" --desc "抠图、换背景，交付小程序。"
python main.py serve        # http://127.0.0.1:8090
```

---

# M2 —— 可配置真实接口 + 安全审批 + 审计 + 榜单

## 概念
- **配置驱动 + 兜底**：judge/worker 用 `config.json`+`.env` 决定；默认 mock 兜底，开箱即跑。
- **真 LLM-as-judge 正确姿势**：锚点 rubric + **盲评**(不透露 worker) + **引用证据** + 严格 JSON 输出。
- **真实 CLI worker**：用"写文件/读文件"而非捕获 stdout 管道（受限环境会 EPERM）。
- **安全审批流(human-in-the-loop)**：敏感/超支 → 停在"待盖章"，你批准才继续。
- **审计 + 画像**：每一步可查；跨任务聚合"谁擅长什么"。

## 新增
```
core/config.py   配置读取(config.json + .env, 默认mock)
judge/api.py     真 LLM-as-judge(urllib, 无key自动回退)
workers/cli.py   真 CLI worker(文件IO)
core/store.py    audit_log / approval / agent_profiles 聚合
```

## 怎样开真接口
- 真 judge：`.env` 填 `DEEPSEEK_API_KEY=`，`config.json` 里 `judge.provider=api`。
- 真 worker：`config.json` 里 `codex`/`dsh` 的 `enabled=true`（命令在 PATH）。
- 没配好会自动回退 mock，**绝不崩**。

---

# M3 —— 状态机 + 断点续跑 + 真实 CLI 驱动

## 概念（agent 可靠性的核心）
- **状态机**：把长任务拆成离散阶段（observe/decide/dispatch/judge/learn）。
- **断点 checkpoint**：每阶段做完就把中间结果持久化 → 中断后能续。
- **恢复 resume**：从断点的下一阶段继续，**不重做已完成/昂贵的阶段**。
- **进程驱动**：真调外部 CLI 用"写文件/读文件"传递输入输出。

## 新增
```
core/pipeline.py   重构为 状态机 + checkpoint + resume
core/store.py      job_state 表(存 stage + 断点)
main.py            --stop-after / --resume；API /api/resume；/api/jobs 带 stage
agents/demo_agent.py  一个"真实可运行"的 CLI agent(证明驱动机制)
```

## 验证（已实测）
```bash
python main.py --run "..." --stop-after dispatch   # 模拟中断 -> interrupted, 断点已存
python main.py --resume JOB-xxxx                    # -> status=done, resumed_from=dispatch(没重做派活)
```
界面：任务 tab 选中断任务 → "从断点恢复"。

---

# M4 —— 学习升级：在线线性策略(L2) + 案例库 RAG

## 概念
- **特征向量 featurize**：把离散特征变成固定长度数值向量。
- **在线线性策略(L2)**：每 worker 一组权重 `W`；分数=`W·features`，选最高。
- **bandit/线性强化**：judge 揭晓实际最强 worker 后，强化赢家权重、惩罚其它。
- **案例库 RAG**：案例还存"踩坑"，决策时检索相似案例把经验带出来。

## 新增
```
learn/policy.py   featurize + PolicyModel + 持久化 + policy_summary
learn/router.py   决策 = L2预测 + L1案例(RAG) + 未练过的类型回退默认规则
```

## 验证（已实测）
跑几个图像/文案任务后：
`history=3, predicts: image->codex, text->minimax`；新图像任务走"策略模型(L2)预测"；
没练过的类型(如 data)回退"默认规则"，**不乱猜**。

---

# M4.5 —— 真正的监督学习：softmax 逻辑回归（梯度下降）

## 概念（让"学习"变成真 ML）
- **softmax 多分类**：`P(用谁 | 特征)`，选概率最高者。比线性策略更"像模型"，能泛化、有准确率。
- **梯度下降 + 交叉熵**：用所有历史案例训练；每次更新往"让正确类概率更高"的方向走。
- **训练/泛化**：模型从多样案例里学出"什么样的特征→用谁"，能推断没见过的新组合。

## 新增
```
learn/model.py   SoftmaxRouter(softmax + 梯度下降, 纯Python)
learn/router.py  案例>=4条就用软max模型；否则线性策略/默认规则
```

## 验证（已实测）
跑 6 个混合类型任务后：`model=softmax, history=6, accuracy=1.0`；
`image->codex, data->mock, text->minimax`（**data 从"乱猜codex"变成正确 mock**——学会的强证据）；
新文案任务决策走 `softmax模型(L2)·准确率1.0 + 案例记忆(L1)`。

---

## 你现在能做什么
- 开 `python main.py serve` → 看总览/任务/评测(含"它学会了什么")/审计/审批。
- 命令行跑任务；`--stop-after` 模拟中断、`--resume` 断点续跑。
- 接真接口：配 `.env`/`config.json`。
- 界面里看"它学会了什么"（模型类型+准确率+各类型该用谁）。

# M5 —— 用真正的框架 LangGraph 做状态机

## 概念
- **LangGraph**：一个 agent 编排框架，用 `StateGraph` 显式定义**节点 + 连线**，比手写状态机更规范、可扩展。
- **checkpointer**：按 `thread_id` 记住每个任务的图状态；**同一 thread_id 再次 invoke 就从断点继续**（框架自带的断点续跑）。
- **节点复用**：把 M3 写的阶段函数（`do_observe/decide/dispatch/judge/learn`）直接作为图节点，数据流交给 LangGraph。

## 新增
```
core/graph.py   StateGraph(observe→decide→dispatch→judge→learn) + MemorySaver checkpointer + run_graph_task
core/pipeline.py run_task 支持"优先用 LangGraph，否则回退手动状态机"
main.py           /api/run 走 _run_or_graph(优先 LangGraph)
```
可选依赖：`pip install langgraph`（装了用框架，没装自动回退内置状态机）。

## 验证（已实测）
- `run_graph_task(...)` 跑通：status=done、leaderboard、案例、策略都产出 ✅
- **修复了一个潜伏 bug**：M3 重构时 `do_judge` 没把每 worker 打分写入 evaluation 表，导致"Agent 画像/跨任务榜单"一直为空；现已修复。
- `/api/run` 走 LangGraph；`/api/leaderboard` 正常（codex avg 4.67）；softmax 学习 acc=1.0 ✅

## 概念补充：为什么用框架
- 显式图：新增节点/加"条件边"(if/循环)/并行分支都很直观。
- 自带 checkpoint、可以加人机审批节点(interrupt)。
- 更接近企业里真实 agent 工程的做法，面试可讲"我用 LangGraph 编排多 agent"。

---

## 你现在能做什么
- `python main.py serve` → 界面：总览/任务/评测(含"它学会了什么")/审计/审批。
- 命令行 `--run` / `--stop-after` / `--resume`（手动状态机路径，教学用）。
- 接真接口：配 `.env`/`config.json`；`pip install langgraph` 启用框架状态机。

# Vue 签发台 + sklearn 路由

## 概念
- 前端用 Vue 3 还原签发台（总览/任务/评测/审计/审批/设置），`vite` 把 `/api` 代理到 Python `:8090`。
- 评测页：榜单用均分横条，画像用均分圆环 + 样本条。
- 路由：有 `scikit-learn` 时用多项式逻辑回归；否则纯 Python softmax。

## 新增
```
frontend/                 Vue 3 + Vite
learn/sklearn_model.py    可选 sklearn 逻辑回归（接口对齐 SoftmaxRouter）
src-tauri/                桌面壳骨架（本机需 Rust 才能 cargo tauri build）
```

## 里程碑状态
- M1 闭环 ✅  M2 可配置/安全/榜单 ✅  M3 状态机+断点续跑+CLI ✅
- M4 线性策略+RAG ✅  M4.5 softmax 真模型 ✅  M5 LangGraph + Vue ✅
- M5.5 sklearn 路由 + 评测图表 ✅
- 待做：Tauri 真正编成桌面应用（装好 Rust 后处理 `/api` 跨域/代理）
