# PRD — haibala：Agent 决策中枢（Agent Command Center）v1.0

> 状态：评审稿 v1.0
> 产品名：**haibala**（自由职业 agent 决策中枢）。
> 定位：一个**桌面端（Tauri）自托管的"监控 + 决策"中枢**。它站在多个 agent 之上，观察任务、替用户做编排决策（用谁 / 拆不拆 / 优先级 / 预算），通过 rubric 评测每个 agent 的表现，并**边用边学会越来越会做这些决策**。用户能实时看到监控、决策过程和理由，并对安全和权限有完整控制。
> 架构哲学：**插件化**（借鉴 DeepSeek Harness 的插件/技能思想）——worker、judge、路由策略、技能、钩子全部做成可插拔插件，核心稳定、扩展不动核心（见 TDD §4.5）。

---

## 1. 一页摘要

你和一群 agent 一起接活。你负责定方向和拍板，而这个工具是一个"队长/中枢"：它帮你观察、派活、打分、并逐渐学会"什么活该派给谁、要不要拆、先做哪个、花多少钱"。它是**桌面应用**，你自己随时能看监控面板、回放决策、管控安全。它不替代你，而是**让你对"用 AI 干活"这件事更可控、更高产，并且越用越懂你**。

一句话：**"一个会学习的 agent 队长，和一块你能盯着的桌面控制台。"**

---

## 2. 目标用户与使用场景

### 目标用户
- **自由职业/独立开发者**（你）：接 AI 相关单子，需要把活拆给多个 AI 工具，并在质量、成本、进度之间做取舍。
- **进阶用户**：已有 GLM/Claude/Codex/DeepSeek 等 coding agent，想统一监控和选型。
- （v1 只服务"单人""本地运行"，不做多租户/团队协作。）

### 核心场景（user journey）

**S1 — 接单决策**
接新活 → 把需求丢给"中枢" → 中枢观察任务特征（类型/技术栈/规模/复杂度）→ **给出决策**（建议用哪个 agent / 是否拆分并行 / 优先级 / 预算）→ 你审批/微调 → 派活。

**S2 — 干活与监控**
agent 开工后，中枢**实时监控**：谁在跑、进度、token/成本/延迟、有无报错。你在面板上随时看，**决策理由**（为什么选它）也展示出来。

**S3 — 评测与学习**
中枢把同一任务（或同类型任务）给多个 agent 跑，按 **rubric** 打分，形成对比和"谁擅长什么"的画像。**这些分数反过来让中枢学习**：下次遇到类似任务，它会给出更准的决策。你可以在面板看榜单、校准打分。

**S4 — 安全与审批**
涉及不可逆动作（写库/发消息/删文件/高成本）时，中枢**暂停并请求批准**；你查看、批准/拒绝、看审计日志，所有动作可回放。

---

## 3. 产品目标与成功指标

### 目标
1. **让"用多 agent 干活"可控、可解释、可量化**。
2. **让编排决策越来越准**（自我改进，这是差异化）。
3. **让用户对安全与成本有实感掌控**。

### 衡量（成功指标）—— v1 尽量可观测
- **决策采纳率**：用户采纳系统建议（worker/拆分/优先级）的比例。
- **决策准确率**：系统建议所选 worker 的实际 rubric 得分（越高越好，随学习应单调提升）。
- **学习提升**：随案例数增加，同类型任务"最佳 worker 命中率"是否上升。
- **成本控制**：每任务实际成本 vs 预算偏差；超预算任务占比。
- **监控可用性**：用户能否在面板看到每一步状态/理由/审计（覆盖率 100%）。
- **安全事件**：未授权敏感操作次数（应为 0，靠权限+审批+审计保证）。

---

## 4. 功能需求（MoSCoW）

> M=必须(v1必做) / S=应该(v1尽量) / C=可以(后续) / W=不做(明确排除)

### 4.1 任务与决策（核心闭环）
- **M1 建任务/接单**：录入任务（标题、描述、附件/参考、预算上限、Deadline 可选）。支持"任务模板 + 用例"（同任务多输入，便于评测泛化）。
- **M1 特征提取**：从任务自动抽取路由特征（任务类型、技术栈、规模、复杂度、子任务数、是否需多模态等）。
- **M1 决策生成**：基于特征 + 历史学习，输出 **DecisionConfig**：`{worker, split(bool), priority_order[], budget}`，并附**决策理由**（可解释）。
- **M1 审批**：用户可 接受 / 修改 / 拒绝 决策（可配置为"每步都要我批"或"仅敏感/超支时才批"）。
- **M1 执行与分发**：按决策把子任务分发到对应 worker，管理生命周期（pending/running/done/failed），支持并行子任务。
- **M1 结果复用**：失败重试（最大次数）、超时/超支中断、手工干预（换 worker/改优先级）。

### 4.2 监控台（Dashboard）
- **M1 实时状态**：总览（进行中/待批/异常/完成）、每 agent 状态与进度、当前卡在哪个子任务。
- **M1 成本/性能**：每任务及每 worker 的 token 数、成本（$）、延迟、时间线；预算**实时告警**。
- **M1 决策透明**：展示"为什么选它"（理由 + 特征 + 参考案例），可回放决策过程。
- **M1 rubric 对比**：同一任务各 worker 的维度分数 + 加权总分 + 依据（引用片段）。
- **M1 日志与审计**：全链路 trace（谁在何时做了什么、调了哪个模型、花多少），可检索/回放。
- **M1 安全与审批中心**：待批准项、敏感操作告警、权限审批记录。

### 4.3 评测（Rubric / Judge）
- **M1 rubric 定义**：自定义维度 + 权重 + 1-5 锚点描述（可存为模板，按任务类型复用）。
- **M1 judge 打分**：按 rubric 对每个 worker 的产出打分；**盲评**（隐藏 worker 身份）；**引用证据**（要求引用产物片段）；输出加权总分与一句理由。
- **M1 judge provider 可插拔**：`API / 本地(ollama) / 混合`，配置切换（见 TDD §Judge）。
- **S1 两两对比 + 绝打分双模态**：pairwise 与绝对分交叉校准。
- **S1 人工校准**：你对某个产出手工标注分数（ground truth），用于校准 judge 尺度、评估 judge 可信度（judge 一致度/偏差）。

### 4.4 学习 / 自我改进（核心差异化）
- **M1 案例入库**：每次执行后，把 `{任务特征 → 决策 → 各worker 实际得分 → 成本/耗时/坑}` 存为案例。
- **M1 案例检索**：接单时按相似案例推荐决策（L1 记忆式学习）。
- **S1 路由策略学习**：用积累数据训练一个轻量路由模型（决策树/小模型），泛化到新任务（L2）。冷启动用人工种子基线。
- **C1 RAG 记忆库**：跨会话保存"决策知识"（什么坑、怎么验收），供用户查询。

### 4.5 多 agent 集成（适配层）
- **M1 适配协议**：统一接口 `submit(task)->artifact`；三种模式：`CLI/API`、`MCP`、`GUI手动`。
- **M1 内置 worker**：DeepSeek Harness（dsh）、Codex CLI（自动）；Cursor（混合/手动兜底）；预留 MCP 通用接入。
- **M1 手动任务卡**：GUI 工具（Coze/MiniMax/Cursor界面）走"控制台发任务卡→人工完成→登记"。
- **S1 worker 注册表**：配置文件新增 worker，无需改核心代码（填配置+一个薄适配器即可）。
- **S1 插件化扩展**：worker / judge / 路由策略 / 技能 / 钩子全部做成**插件**（借鉴 DeepSeek Harness 思想），核心稳定、加能力只加插件（见 TDD §4.5）。

### 4.6 安全与权限
- **M1 权限模型**：每个 worker/动作有权限等级；敏感/不可逆/高成本动作需**显式审批**；最小权限。
- **M1 沙箱/隔离**：worker 在独立工作区（临时目录、受限网络、受限进程）运行，防互相污染、可审计。
- **M1 密钥管理**：API key/凭证用 OS 密钥链（Tauri 插件）保存，不进明文配置。
- **M1 防注入**：外部内容（抓取网页、工具返回、客户文档）进入 prompt 前分隔转义 + 护栏校验；防止 prompt injection 提权/泄密。
- **M1 审计日志**：所有动作、审批、越权尝试记录，可回放（不可篡改/可导出）。

### 4.7 桌面端（Tauri）
- **M1 桌面壳**：窗口/系统托盘/通知；Tauri 侧边栏 + WebView 监控 UI。
- **M1 Python 核心 sidecar**：Tauri 负责拉起、监控、崩溃重启 Python 核心服务（本地 HTTP + WebSocket）。
- **S1 常驻/自启动**：可选择开机自启、最小化到托盘、后台持续监控。
- **C1 打包分发**：Windows 安装包（后续 macOS/Linux），开源发布。

### 4.8 配置与偏好
- **M1 全局设置**：judge provider、预算默认值、审批策略（哪类动作需要我批）、worker 列表与可用性、模型路由偏好。
- **M1 项目级配置**：每个任务可覆盖全局（预算、worker 白名单、rubric 模板、审批级别）。

---

## 5. 主用户流程

```
[新建任务] 录入需求
   → [特征提取] 自动抽取类型/技术栈/规模/复杂度
   → [决策生成] supervisor: {worker, split, priority, budget} + 理由
        ├─ 采用 ── 派活
        └─ 修改/拒绝 → 重跑决策或人工改
   → [分发执行] 并行/串行派给各worker → 实时状态上报
        ├─ 需要审批动作(敏感/超支) → 暂停等用户批准
        └─ 失败/超时/超支 → 重试或人工干预
   → [评测] rubric 打分(judge) → 对比/榜单
   → [学习] 写入案例 → 更新画像 → (L2)更新路由策略
   → [监控] 用户随时看状态/成本/理由/安全/审计
   → [交付/入库] 产物汇总 → 验收 → 结束
```

---

## 6. 信息架构（v1 页面）

- **总览 Dashboard**：卡片（进行中/待批/异常/完成），全局预算与告警。
- **任务详情**：特征 / 决策与理由 / 子任务面板（每个 worker 状态+产物）/ 成本时间线 / 审批记录 / 审计。
- **评测/榜单**：rubric 定义编辑器；对比视图；各 agent 维度分数与画像；校准区。
- **监控/审计**：全链路日志、事件时间线、回放、导出。
- **安全/审批**：待批准项列表、权限规则、敏感动作记录、密钥管理。
- **设置**：judge provider、预算默认、审批策略、worker 注册表、模型路由。

---

## 7. 非功能需求（NFR）

- **性能**：监控事件近乎实时（<2s 延迟）；本地核心服务启动 <5s；任务状态刷新不阻塞 UI。
- **可靠性**：核心服务崩溃后可自动重启并**断点续跑**（checkpoint）；无数据丢失。
- **可解释性**：每个决策有理由 + 引用证据 + 参考案例；可回放。
- **安全/隐私**：默认本地优先；数据不出本机（除非用户用 API judge/worker）；密钥加密存储；审计不可篡改。
- **成本**：跟踪每步 token/成本；预算告警；可选便宜模型路由。
- **可扩展性**：worker/judge 均"配置即插"，不侵入核心。
- **用户体验**：中文界面；交互简洁；重要事件即时通知（托盘/系统通知）。

---

## 8. v1 范围与边界（明确不做）

- ❌ **不做画布/节点编排 UI**（后面版本，v1 用任务/子任务列表呈现）。
- ❌ **不做自动报价**（报价权在人）。
- ❌ **不做模型训练/微调**（只做编排+学习闭环，学习用轻量策略模型/案例库）。
- ❌ **不做多租户/团队协作/云端 SaaS**（v1 单机本地）。
- ❌ **不强行自动化 GUI 工具**（GUI 走手动登记，避免脆弱）。
- ❌ **不做全自动无人监督**（核心是"可控的半自动"）。

---

## 9. 版本计划

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1 (v1)** | 监控+决策闭环：建任务→特征→决策(理由)→分发→状态监控→rubric打分→案例入库→检索推荐 | 一条真实"编码类"任务全流程跑通，面板可见 |
| M2 | 多 worker（含 MCP/手动）+ 并行子任务 + 预算/超时控制 | 2-3 个 worker 并行 + 成本告警 |
| M3 | judge 可靠性（盲评/引用证据/pairwise/人工校准） | judge 一致度可量化、可校准 |
| M4 | 学习闭环 L1→L2（路由策略）+ 画像/榜单 | 同类型任务最佳 worker 命中率随数据上升 |
| M5 | 桌面端完整化（托盘/常驻/通知/打包）+ 安全加固 | Windows 安装包可用、安全审计无越权 |

> v1 = M1，聚焦"**能不能用起来并看到价值**"。

---

## 10. 开放问题 / 待定

1. 任务/案例的**存储规模**预期（决定 sqlite 还是 pg）。
2. 是否需要在 v1 就支持**手动任务卡**（GUI 工具手动登记）还是先只上 CLI 自动化。
3. 预算单位/货币（￥/$）、是否按 API 单价自动折算。
4. 是否需要"多任务并行管理"（同时跑多个独立项目）还是 v1 单任务。
5. 项目**开源许可**（建议 MIT，与 Nimbalyst 一致）与是否打独立品牌名。
6. 首版支持的语言：仅中文界面，还是中英双语。
