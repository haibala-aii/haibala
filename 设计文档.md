# Agent 评测与编排台（管理 Agent 的 Agent）— 设计文档 v0.2

> 状态：草稿，供人工评审修改
> 定位：一个 **meta-agent（supervisor）**，它管理多个工人 agent，并且能**定义 rubric(评分标准)、把同一任务派给多个 agent、打分对比、形成榜单与"谁擅长干什么"的画像**，最后**指导下次该用哪个 agent**。

> 一句话：**"我派活，它管理；顺手告诉我哪个 agent 干这个更好、好多少、为什么，然后记住。"**

---

## 0. 核心要回答的问题

1. **这个任务，我用哪个 agent（Codex / DeepSeek Harness / MiniMax …）做最好？**
2. **好多少？好在哪个维度（正确性？质量？成本？速度？）？**
3. **为什么（judge 引用证据/理由）？**
4. **长期来看，每个 agent 的"能力画像"是什么？**（agent A 擅长逻辑编码、B 擅长设计、C 便宜但质量一般）
5. **这套画像能不能反过来指导我的日常接单/干活选型？**

---

## 1. 为什么是这个（评审背景）

- 这是当前 Agent 领域**最空白、最稀缺**的一环：**评估（evaluation / LLM-as-judge）**。会做"agent 评测/benchmark/rubric"的人非常少，而且正是企业最头疼（"你凭什么说你这个 agent 好"）。
- 命中你之前认可的三块核心：**可靠性（评估打分）、多 agent 编排（管理 + 调度）、控制台**。
- 对你个人**真实有用**：你是自由职业者，手上有多个 AI 工具，这套东西能帮你**决策"接单的哪种活该派给哪个工具"**，省时间、少踩坑。
- 它是** meta-agent + benchmark + judge** 三合一，简历上极有区分度。

---

## 2. 整体架构

```
┌───────────────────────────────────────────────────────────────┐
│ 控制台 Dashboard(Web)   ←→ 我看榜单/单次对比/校准/审批            │
└───────────────▲─────────────────┬─────────────────────────────┘
                │ 查看/校准/上报      │ 下发评测任务
┌───────────────┴─────────────────▼─────────────────────────────┐
│ 编排核心 Supervisor (LangGraph 状态机)                           │
│  config → dispatch(并行派给多工人) → collect → judge → report → archive │
└──────┬──────────┬────────────┬────────────┬────────────────────┘
       │          │            │            │
  ┌────▼────┐ ┌───▼────┐ ┌─────▼─────┐ ┌────▼─────┐
  │任务构造  │ │Rubric  │ │Judge Agent│ │记忆/画像  │
  │同一任务   │ │定义/权重│ │LLM打分+校准│ │案例/agent profile│
  └─────────┘ └────────┘ └───────────┘ └──────────┘
       │
  ┌────▼──────────────────────────────────────────────┐
  │ 工人适配层 Worker Adapter (可插拔)                    │
  │  统一接口 submit(task)->artifact                    │
  │  CodexCLI │ DSH SDK │ MiniMax API │ [手动工人]      │
  └────────────────────────────────────────────────────┘
```

**核心思想**：Supervisor 是大脑，工人是"同一份考卷的多个答题者"。**同一个任务**（或任务模板的多个用例）分发给多个工人，收集各自的"答案"，再用 **judge** 按 rubric 打分，得出对比。

---

## 3. 核心流程：怎么跑（端到端）

### 3.1 状态机（LangGraph）

```
config → run → dispatch(同一task给N个工人，_并行_) → collect(all artifacts)
       → judge(逐agent 按rubric 打分) → report(对比榜单)
       → archive(写案例+更新agent画像)
任意时刻: 控制台 interrupt，等 我批准/校准judge/中止
```

> 可**断点续跑**：因为状态机 + 每步产物落盘，跑一半中断能从 checkpoint 恢复（比如某工人跑了很久）。

### 3.2 一个示例完整走一遍

**样例场景**：给 3 个 agent 同一个任务——"写一个 Python 函数，从一堆商品图里批量抠图并输出背景替换后的图"，看谁做得好。

#### Step 0 — 配置一个评测（config）
- 我建一个 **Benchmark**：绑定 `task` + `rubric` + 参与工人。
- task 可以是**一个任务**，也可以是**一个任务 + 多个用例**（不同样例输入，避免过拟合单个输入）。

#### Step 1 — 定义 rubric（评分标准）
我（或 agent 先给草案再我改）定义评分维度 + 权重 + 锚点描述：

```jsonc
{
  "rubric_id": "R-coding-img",
  "title": "图像处理编码任务评价",
  "dimensions": [
    {"name":"功能正确性","weight":0.4,
     "scale":5,
     "anchors":{"5":"完全满足需求并能跑通","3":"核心功能可用,边缘有缺陷","1":"基本不可用"}},
    {"name":"代码质量","weight":0.25,
     "scale":5,
     "anchors":{"5":"清晰/可维护/有注释与错误处理","3":"可读,有些冗余","1":"混乱/无结构"}},
    {"name":"可复用性","weight":0.2,"scale":5,"anchors":{}},
    {"name":"成本/耗时","weight":0.15,"scale":5,"anchors":{}}
  ]
}
```
> **Rubric 是关键**：有明确锚点（anchor）的 rubric 是 judge 可靠的前提。没有锚点的打分=拍脑袋。

#### Step 2 — 分发（dispatch）
- Supervisor 把同一个 task + 同一组用例，**并行**发给 3 个工人（Codex / DSH / MiniMax / 或手动工人）。
- 每个工人跑在自己的**隔离工作区**，产物落盘到 `runs/<benchmark_id>/<worker>/`。

#### Step 3 — 收集（collect）
- 收齐 3 份产物（代码/说明/日志/耗时/成本）。
- 归一化成同一可对比的形态（都返回 `artifact|代码文件|运行日志|备注`）。

#### Step 4 — Judge 打分（judge）—— 核心难点
- **LLM-as-judge**：给 judge 每份产物 + rubric，让它按维度打分并**引用证据**。
- **盲评**：隐藏"这是哪个 agent 做的"，防止自偏好/地位偏见。
- **反对齐**（可选）：除绝对打分外，再做**两两对比**（A vs B 谁更好）交叉验证。
- **人工校准**：我可以在控制台 override 某维度的分，作为 judge 的"ground truth"，帮助 judge 校准。
- 输出：每 agent × 每维度分数 + 加权总分 + 一句理由。

#### Step 5 — 对比报告（report）
控制台给出：
```
榜单(按加权总分):
 1. Codex    4.2  正确性:5  质量:4  复用:4  成本:3.5
 2. DSH      3.8  正确性:4  质量:4  复用:3.5 成本:4
 3. MiniMax  2.9  正确性:3  质量:2.5 复用:2.5 成本:3
差异分析: Codex 在正确性/质量上领先; MiniMax 在该"编码类"任务上明显弱。
```

#### Step 6 — 记忆/画像（archive）
- 把这次评测写入**案例库** + 汇总到 **AgentProfile**：
  ```
  AgentProfile[codex]: {平均分:4.2, 擅长:[coding,logic], 弱:[design], 平均成本/耗时, sample:...}
  AgentProfile[minimax]: {平均分:2.9, 擅长:[design,video], 弱:[coding], ...}
  ```
- **下次接单**：如果你要做"图像处理类"的活，supervisor 检索画像 → 建议"用 Codex"; 做"设计类" → 建议"用 MiniMax"。

---

## 4. 数据模型（初稿）

```jsonc
// Benchmark（一次评测 = "一份考卷"）
{ "benchmark_id":"B-...", "title":"图像编码评测", "task":{...}, "use_cases":[...] }

// Rubric（评分标准）
{ "rubric_id":"R-...", "dimensions":[{"name","weight","scale","anchors"}] }

// Run（一次分发执行 = "一个考生答题"）
{ "run_id":"...","benchmark_id":"...","worker":"codex|dsh|minimax|manual",
  "artifact_path":"runs/B-x/codex/","status":"running|done|failed",
  "cost":0.31,"latency_ms":8200 }

// Evaluation（打分结果）
{ "eval_id":"...","run_id":"...","rubric_id":"...",
  "scores":{"功能正确性":5,"代码质量":4,...},
  "weighted_total":4.2,"rationale":"...", "judge":"primary|secondary|human_override" }

// AgentProfile（长期画像，按工人聚合）
{ "worker":"codex", "avg_score":4.2,
  "strengths":["coding","logic"], "weaknesses":["design"],
  "avg_cost":0.28,"avg_latency_ms":9000, "sample_eval_ids":[...] }

// Case（案例条目，供接单时检索）
{ "case_id":"...", "task_type":"图像编码", "stack":["opencv","rembg"],
  "best_worker":"codex","scores":{...},"pitfalls":["背景生成不稳"] }
```

---

## 5. 工人适配层（复用，简化）

所有工人统一实现一个薄接口，Supervisor 不关心内部：

```python
class WorkerAdapter(ABC):
    name: str
    def submit(self, task: Task, workspace: Path) -> Artifact: ...
    def status(self, task_id: str) -> WorkerStatus: ...
    def cancel(self, task_id: str): ...
```

- **自动工人**：Codex CLI、DeepSeek Harness SDK、MiniMax API …（各自封装 CLI/SDK 调用）。
- **手动工人**：Coze / Cursor 界面 / MiniMax 网页 → 控制台发任务卡，我干完点"完成"。
- **沙箱**：每工人独立临时工作区 + 受限网络，防污染、可审计。不可逆动作需我显式批准。

> 边界提醒：自动工人调用 CLI 时**不要捕获 stdout 管道**（受限环境会 EPERM），用"写文件 + 读文件"或 `stdio:inherit` 取结果；control 与 worker 之间以文件/事件传递。

---

## 6. Judge（LLM-as-judge）可靠性设计 ⚠️（本项目的硬骨头）

这是最能体现水平、也最容易被质疑的部分，优先设计好：

1. **Rubric 带锚点**：每维度 1-5 有明确"什么样的给5分、给1分"的描述。
2. **盲评**：隐藏 worker 身份，防止模型对知名工具"高看一眼"。
3. **引用证据**：要求 judge 打分时**引用产物的具体片段**作为依据，防止空口打分。
4. **两两对比 + 绝对打分双模态**：先 pairwise 见高下，再绝对分定距离，交叉校准。
5. **多 judge 集成**：用多个不同模型/多次采样取均值，降低单次波动与自偏好。
6. **人工校准集**：我标几条 ground truth，校准 judge 的尺度偏移，并输出"judge 可信度"。
7. **去偏**：警惕 verbosity bias（话多≠好）、长度偏好、位置偏好。

> 诚实提醒：LLM-as-judge 会**系统性偏置**，不可能 100% 准。我们的目标是**相对排序稳定、可解释、可校准**，而不是绝对真理。这一点当卖点还是风险，取决于你把"judge 校准"做得是否扎实。

---

## 7. 控制台（Dashboard）

一个 Web 界面（FastAPI + 前端）：

- **榜单 / Leaderboard**：各 agent 在各类任务上的加权总分、成本、耗时。
- **单次对比**：选中一次 benchmark，看同一任务各 agent 的产物 + 各维度分数并排。
- **Rubric 编辑**：加维度/改权重/写锚点。
- **Judge 校准**：看 judge 打分 vs 我的 ground truth，手动 override，校准尺度。
- **Agent 画像**：每 worker 擅长什么/弱在哪/成本延迟。
- **手动任务卡**：派给 GUI 工具的活，我干完登记。
- **审计日志 / 断点续跑**：每一步状态、成本、可恢复。

---

## 8. 技术选型（待你定，TBD）

- 语言：**Python**（LangGraph 生态最顺）或 **TypeScript**（LangGraph.js）—— ⚠️
- 编排：**LangGraph**（状态机 + checkpoint）
- 存储：**sqlite + faiss**（数据量小够用），或 **postgres + pgvector**（更大）—— ⚠️
- 控制台：**FastAPI + 轻量前端**，或先 **CLI + 简单网页**—— ⚠️
- 工人调用：各工具 CLI/SDK 适配器
- 配置：`.env` 管 API key 与工具路径；进程/容器隔离工作区

---

## 9. 目录结构

```
agent-pm/
├─ design.md
├─ README.md
├─ .env.example
├─ core/                # Supervisor 状态机
│  ├─ state.py / graph.py / store.py
├─ bench/               # 评测编排
│  ├─ config.py         # Benchmark + Rubric 定义
│  ├─ dispatch.py       # 派任务给多工人(并行)
│  └─ collect.py        # 收产物/归一化
├─ judge/               # LLM-as-judge
│  ├─ rubric.py         # 锚点/权重
│  ├─ judge.py          # 盲评/引用证据/pairwise
│  └─ calibrate.py      # 人工校准/去偏
├─ workers/             # 工人适配器
│  ├─ base.py / codex.py / dsh.py / minimax.py / manual.py
├─ profile/             # AgentProfile + 案例库(RAG检索)
├─ dashboard/
├─ data/                # sqlite / faiss / 案例库
└─ runs/                # 每次运行的产物/日志/checkpoint
```

---

## 10. MVP 里程碑（先跑通再扩）

- **M1（跑通一条评测链）**：定义 rubric → 同一任务派给 **2 个自动工人** → collect → judge 打分 → 出对比榜单。人工手动改 rubric 即可。
- **M2（多工人 + 手动工人）**：接入 3-4 个 worker（含手动工人任务卡），支持**并行**。
- **M3（judge 可靠性）**：盲评 + 引用证据 + pairwise + 人工校准 + judge 一致度。
- **M4（记忆/画像）**：AgentProfile 累积 + 案例库 RAG，接单/派活时**自动给出选型建议**。
- **M5（控制台 + 安全）**：Dashboard 完整化 + 沙箱 + 断点续跑 + 成本监控。

> 建议 M1 先落地并演示（**2 个 agent 同题对比 + rubric 榜单**已经非常能打）。

---

## 11. 风险与避免

| 风险 | 应对 |
|---|---|
| Judge 打分不可靠/偏置 | 锚点 rubric + 盲评 + 引用证据 + 人工校准，定位为"相对排序稳定"而非绝对真理 |
| 多 worker 统一难 | 只自动接 CLI/API；GUI 当手动工人 |
| 范围大烂尾 | 严格按 M1→M5，先 2 个工人跑通一条链 |
| 一次性任务过拟合 | task 用多用例；榜单看平均而非单次 |
| 成本爆炸 | 每步记录成本/延时 + 预算告警 |
| 环境 subprocess stdout EPERM | 改用写文件/读文件或 stdio:inherit |

---

## 12. 需要你确认/修改的点（TBD）

1. 技术栈：**Python** 还是 **TypeScript**？
2. 第一个参与对比的 **2 个工人**：Codex / DeepSeek Harness / MiniMax 先接哪两个？
3. 第一个评测的**任务类型**：编码类？图像处理类？文案/设计类？（先挑你最常接单、最好打分的）
4. 库：sqlite+faiss 还是 postgres+pgvector？
5. 控制台形态：先 CLI，还是直接 Web Dashboard？
6. 项目命名：Agent-Judge / Benchmark Hub / 其它？

---

## 附：为什么这个比"接单流水线"更值得做

- 它把你从"**用 AI 干活的人**"变成"**研究怎么用 AI 的人**"——后者正是市场上稀缺、且有门槛的方向。
- **评估/benchmark/judge** 是 Agent 落地最硬的一环，会做的人少，面试极有区分度。
- 同时它**反哺你的自由职业**：接单前查一眼画像就知道"这种活派给谁、怎么验收"，直接省时间。
