<template>
  <div class="paper panel">
    <h2>设置</h2>
    <p class="desc">Judge：读 config.json（provider = mock | api）</p>
    <p class="desc">预算阈值：$2.0（超过即"待盖章"）</p>
    <p class="desc">Workers：config.json 里把 codex/dsh 的 enabled 改 true 即接真命令（需命令在 PATH）</p>
    <p class="desc">可依赖：pip install langgraph（状态机）· pip install scikit-learn（更强路由模型）</p>
  </div>
</template>
